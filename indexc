<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Riddhi — NEET 2026 Motivation</title>
  <style>
    body {
      font-family: 'Poppins', sans-serif;
      background: linear-gradient(135deg, #ffdde1, #ee9ca7, #a1c4fd, #c2e9fb);
      background-size: 400% 400%;
      animation: gradientBG 12s ease infinite;
      text-align: center;
      padding: 20px;
      margin: 0;
      overflow-x: hidden;
    }

    @keyframes gradientBG {
      0% { background-position: 0% 50%; }
      50% { background-position: 100% 50%; }
      100% { background-position: 0% 50%; }
    }

    h1 {
      font-size: 2rem;
      color: #222;
      margin-bottom: 10px;
    }

    p {
      font-size: 1rem;
      color: #333;
      max-width: 90%;
      margin: auto;
      line-height: 1.6;
    }

    h2 {
      margin-top: 20px;
      font-size: 1.4rem;
      color: #222;
    }

    .signature {
      font-style: italic;
      margin-top: 5px;
      color: #555;
    }

    .button {
      margin-top: 25px;
      padding: 15px 40px;
      font-size: 1.1rem;
      border: none;
      border-radius: 40px;
      background: linear-gradient(45deg, #ff416c, #ff4b2b);
      color: white;
      font-weight: bold;
      cursor: pointer;
      transition: 0.3s;
    }
    .button:hover {
      transform: scale(1.05);
      background: linear-gradient(45deg, #ff4b2b, #ff416c);
    }

    .loading-box {
      margin-top: 30px;
      font-size: 1.1rem;
      font-weight: bold;
      color: #222;
      display: none;
    }

    .dots::after {
      content: '';
      animation: dots 1.5s steps(5, end) infinite;
    }
    @keyframes dots {
      0%, 20% { content: ''; }
      40% { content: '.'; }
      60% { content: '..'; }
      80%, 100% { content: '...'; }
    }

    .result-box {
      margin-top: 40px;
      display: none;
      background: white;
      padding: 20px;
      border-radius: 15px;
      box-shadow: 0 4px 15px rgba(0,0,0,0.2);
      font-size: 1.3rem;
      font-weight: bold;
      color: #008000;
      animation: popIn 0.6s ease forwards;
    }

    @keyframes popIn {
      from { transform: scale(0.5); opacity: 0; }
      to { transform: scale(1); opacity: 1; }
    }

    .confetti {
      position: fixed;
      width: 8px;
      height: 8px;
      top: -10px;
      border-radius: 50%;
      animation: fall linear forwards;
      z-index: 999;
    }
    @keyframes fall {
      to {
        transform: translateY(100vh) rotate(720deg);
      }
    }

    /* Make everything responsive */
    @media (max-width: 600px) {
      h1 { font-size: 1.6rem; }
      h2 { font-size: 1.2rem; }
      p { font-size: 0.95rem; }
      .button { padding: 12px 30px; font-size: 1rem; }
      .result-box { font-size: 1.1rem; padding: 15px; }
    }
  </style>
</head>
<body>

  <h1>A little place for you, Riddhi 💖</h1>
  <p>
    I know Riddhi you did your best this year but luck was not on a greener side for you this year and you were just 2 questions short of getting a seat in government medical college.  
    Next year you will surely crack and get <b>AIIMS Bhubaneswar 😉</b>
  </p>

  <h2>Riddhi Rupasi</h2>
  <p class="signature">— With love, Adarsh Shandilya</p>

  <button class="button" onclick="revealResult()">Reveal NEET 2026 ✨</button>

  <div class="loading-box" id="loading">Loading NEET 2026 Result<span class="dots"></span></div>
  <div class="result-box" id="result">🎉 NEET 2026 Result: <br> Riddhi Rupasi — Triple Digit Rank Achieved 🎉</div>

  <script>
    function revealResult() {
      document.getElementById("loading").style.display = "block";

      setTimeout(() => {
        document.getElementById("loading").style.display = "none";
        document.getElementById("result").style.display = "block";
        launchConfetti();
      }, 4000);
    }

    function launchConfetti() {
      for (let i = 0; i < 150; i++) {
        let confetti = document.createElement("div");
        confetti.classList.add("confetti");
        confetti.style.left = Math.random() * window.innerWidth + "px";
        confetti.style.backgroundColor = `hsl(${Math.random() * 360}, 100%, 50%)`;
        confetti.style.animationDuration = (Math.random() * 3 + 2) + "s";
        document.body.appendChild(confetti);

        setTimeout(() => { confetti.remove(); }, 6000);
      }
    }
  </script>

</body>
</html>
